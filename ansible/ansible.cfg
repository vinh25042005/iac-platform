[defaults]
host_key_checking = False
interpreter_python = /usr/bin/python3
timeout = 60
retry_files_enabled = False
stdout_callback = yaml
roles_path = ./roles

# Inventory thường được truyền qua -i khi chạy (từng project×env).
# Giá trị mặc định chỉ dùng khi chạy ansible tay không có -i.
inventory = inventories/techshop-stg.ini

[ssh_connection]
# BẮT BUỘC cho worker private subnet (đi qua ProxyJump master):
# ForwardAgent=yes để key local được forward tới worker.
# ServerAliveInterval + CountMax: SSH gửi keepalive mỗi 30s và tự thoát nếu
# mất kết nối 3 lần liên tiếp (chống treo vô hạn → ansible treo như cũ).
# ConnectTimeout rút về 15s để fail nhanh khi node chưa SSH-ready.
ssh_args = -o ForwardAgent=yes -o ControlMaster=auto -o ControlPersist=60s \
  -o ServerAliveInterval=30 -o ServerAliveCountMax=3 -o ConnectTimeout=15
