apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bnf-stg
  namespace: argocd
  finalizers: [resources-finalizer.argocd.argoproj.io]
spec:
  project: default
  source:
    repoURL: https://github.com/<your-org>/iac-platform.git
    targetRevision: main
    path: helm/_base
    helm:
      valueFiles:
        - values/bnf/values.yaml
        - values/bnf/values-stg.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: bnf-stg
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions:
      - CreateNamespace=true
